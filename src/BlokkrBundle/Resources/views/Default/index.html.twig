{% extends '::base.html.twig' %}

{% block body %}
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        {{ include('BlokkrBundle:Default:_index_authenticated.html.twig') }}
    {% else %}
        {{ include('BlokkrBundle:Default:_index_unauthenticated.html.twig') }}
    {% endif %}
{% endblock %}